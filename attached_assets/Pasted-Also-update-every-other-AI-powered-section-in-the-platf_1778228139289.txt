Also update every other AI-powered section in the platform so the entire application becomes fully VPS-compatible and does not depend on Replit AI Integrations or OpenAI APIs.

Additional changes required:

## Remove AI API Usage Everywhere

Search the entire project for:

* OpenAI SDK usage
* `chat.completions.create`
* `AI_INTEGRATIONS_OPENAI_BASE_URL`
* `AI_INTEGRATIONS_OPENAI_API_KEY`
* any GPT model references
* any AI helper/util/service files

Replace all such functionality with local backend logic.

---

# 1. AI Summary in New Registration

Current behavior:

* Uses GPT/OpenAI to generate farmer summary/profile insights.

New behavior:

* Generate summaries using backend template logic.

Example:

* Build summaries dynamically using farmer data fields.

Example output:

* "Small-scale farmer from Nashik cultivating onions and tomatoes with limited irrigation access and annual income below ₹2 lakh. Likely eligible for subsidy and irrigation support schemes."

Implementation requirements:

* Use conditional text generation.
* Use reusable helper functions/templates.
* No AI APIs.

Add:

* risk indicators
* eligibility highlights
* profile classification
* farming type classification

Example classifications:

* Marginal Farmer
* Small Farmer
* Irrigated Farmer
* Rain-fed Farmer
* High Subsidy Priority

---

# 2. AI in Application Section

Current behavior:

* AI assists in scheme application recommendations or form filling.

New behavior:

* Replace with rule-based automation.

Required logic:

* Auto-fill suggestions using existing farmer profile data.
* Detect missing documents.
* Detect incomplete fields.
* Recommend required uploads based on scheme requirements.
* Show validation warnings.

Add:

* application completeness percentage
* document checklist
* eligibility confidence score
* missing requirement alerts

Everything should be generated using backend logic only.

---

# 3. AI in Grievance Section

Current behavior:

* Uses AI for grievance classification/reply generation.

New behavior:

* Implement local classification logic.

Requirements:

* Categorize grievances using keyword matching and rules.

Example categories:

* Payment Delay
* Scheme Rejection
* Document Issue
* Technical Problem
* Application Status
* Land Verification
* Bank Account Issue

Add:

* priority detection
* urgency score
* auto-routing suggestions
* predefined response templates

Example:

* If message contains:

  * "payment not received"
  * "stipend pending"
  * "money not credited"

Then:

* Category = Payment Delay
* Priority = High

No AI APIs should be used.

---

# Architecture Requirement

Create reusable backend services:

* `/services/recommendationEngine`
* `/services/summaryGenerator`
* `/services/grievanceClassifier`
* `/services/applicationAnalyzer`

Use:

* scoring systems
* keyword matching
* weighted rules
* helper utilities
* reusable constants/configs

---

# Important

The entire platform must work:

* on a normal VPS
* with PM2
* without Replit services
* without internet dependency for AI
* without OpenAI billing/API keys

Keep:

* existing frontend UI
* endpoint structure
* API responses
* user experience

Only replace the backend intelligence layer with deterministic local logic.
